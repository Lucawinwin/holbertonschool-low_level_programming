#include "main.h"
#include <stdio.h>

/**
 * imprime nombre de 0 a 9
 */

int _putchar (char c) {
return (putchar) (c)}

void print_numbers(void)
{
char [10] = "0123456789";

for (int i = 0; i < 10 ; 1++)
{
_putchar(numbers[i]);
}

_putchar('\n');

return (0);
